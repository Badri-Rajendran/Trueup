Fill every `<…>` placeholder. Inline referenced files with `$(cat path)` or
`$(sed -n 'A,Bp' path)` — do not only name a path and hope it gets read. Run every command in this
template from the **Trueup repo root** (`/Users/badrinarayanan/Codes/projects/Trueup`).

---

You are implementing a slice of Trueup, a regulated retail-investing platform. This repo has two
stacks: `backend/` (Flask + SQLAlchemy 2 + PostgreSQL) and `frontend/` (Vite + React 19).

## Stack: <backend | frontend>

**If backend** — read this before writing any code, pasted in full so it cannot be skipped:

**backend/CLAUDE.md:**
$(cat backend/CLAUDE.md)

**The owning spec section (<docs/specs/N-*.md> §<X>):**
$(sed -n '<start>,<end>p' docs/specs/<N-name>.md)

**The ADR(s) it cites, if any:**
$(cat docs/decisions/<NN-name>.md)

**If frontend** — read this before writing any code:

**frontend/CLAUDE.md:**
$(cat frontend/CLAUDE.md)

<no owning spec section yet for frontend work — omit that block above; describe the task directly>

## Task

<one paragraph: what to build. For backend, the FR/NFR id(s) it satisfies.>

## Files you own — create or edit only these

<exact paths>

## Files you must not touch

Everything else. In particular: nothing under `docs/`, no `.env`, no `pyproject.toml`/
`package.json` (ask first if a dependency is genuinely needed — see the one pre-authorized
exception below). If another slice is running in parallel, name its files here so you never
collide with it.

## Non-negotiables

**Universal, both stacks:**
- `uv run` (backend) or the project's own `npm run` scripts (frontend) for every command — never
  a bare interpreter or a global tool install.
- Follow SOLID, DRY, KISS exactly as stated in the CLAUDE.md pasted above for this stack.
- No `git` operations of any kind. No edits under `docs/`. No touching `.env`.
- **Never leave an AI/agent attribution mark anywhere** — no "Generated by", "Co-Authored-By",
  model name, or similar, in a code comment, docstring, or any file content.

**Backend only:**
- Test-first: no production module before its failing test.
- Real PostgreSQL for anything beyond `tests/unit/` — never SQLite.
- Every money/unit value is `Money`/`Units`/`Price` (`app/core/money.py`) — never a bare `Decimal`.
- Every bitemporal repository read takes `as_of: Watermark`, no default.
- `Enum`, never a bare string, for any DB-backed status column.

**Frontend only:**
- Layer strictly: JSX/CSS for presentation, custom hooks for state and logic, `services/` for the
  outside world. Keep components presentational.
- `useEffect` only to sync with something external — never for derived state or event handling.
- Merge state that always changes together into one value, so impossible UI states cannot exist.
- Vitest + React Testing Library, one test file per component, colocated; cover render, user
  interaction, and loading/empty/error states. **Pre-authorized exception to the dependency rule
  above**: if Vitest/RTL are not yet installed, `npm install -D vitest @testing-library/react` is
  already approved by `frontend/CLAUDE.md` itself — do not ask first for this one install, and do
  not skip testing because the framework isn't there yet.
- Semantic HTML, labelled inputs, keyboard access, visible focus states.

## Definition of done — run these yourself, and paste the real output

**Backend**, from `backend/`:

```sh
uv run pytest -q            # the WHOLE suite — unit, integration, api, contract
uv run mypy --strict app
uv run ruff check app tests
uv run lint-imports
```

**Frontend**, from `frontend/`:

```sh
npm run lint
npm run build
npm test
```

Whichever stack applies, all of its commands must be clean. "Some tests pass" is not done. Do not
write a test whose only purpose is raising a coverage percentage — every test asserts a real
behavior or invariant.

## Report, in this exact structure

1. **Files changed** — each path, one line on why.
2. **Spec/ADR implemented** — which section(s); omit for frontend work with none yet.
3. **Gate output** — the real terminal output of every command above for this stack, not a summary.
4. **Decisions the spec/CLAUDE.md didn't cover** — anything you had to choose yourself.
5. **Double-check** — anything you want a second look at.
6. **Attribution check** — confirm no AI/agent mark was left anywhere in the changed files.

Do not claim done without pasting real gate output. Do not touch git.
